/**
 * Landing Page Configuration
 * 
 * Adjust these settings to customize the landing page appearance.
 * Changes take effect immediately on save.
 */

export const landingConfig = {
  /**
   * Enable/disable floating particles around the hero logo
   * Set to false to remove the particle effect
   */
  showFloatingParticles: true,

  /**
   * Number of particles to display (if enabled)
   * Default: 24
   */
  particleCount: 24,

  /**
   * Enable/disable the spark particles on the final period of "y punto."
   */
  showPeriodSparkles: true,

  /**
   * Enable/disable the repeating shine effect on "y punto."
   */
  showShineEffect: true,

  /**
   * Enable/disable the animated underline on "y punto."
   */
  showUnderline: true,
};
